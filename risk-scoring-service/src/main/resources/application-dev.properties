# Only for isolated local development; JWT and authorization remain mandatory.
risk.security.issuer-uri=${RISK_JWT_ISSUER_URI:http://localhost:8180/realms/DoAn}
risk.security.jwk-set-uri=${RISK_JWT_JWK_SET_URI:http://localhost:8180/realms/DoAn/protocol/openid-connect/certs}
risk.security.require-https=false
spring.data.redis.ssl.enabled=${RISK_REDIS_SSL:false}
